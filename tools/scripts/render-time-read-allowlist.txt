# render-time-read-allowlist.txt
#
# Allowlisted render-time .read() sites in crates/core/src/ui.
# These are sites where .read() appears at the top-level of a render body
# or hook-setup function but the subscription IS the intent (MEDIUM/LOW
# risk). They drive rsx! output, conditional rendering, or child-component
# props -- the component SHOULD re-render when the signal changes.
# HIGH-risk sites (value drives a use_spawn_once / use_reactive_effect key
# or a .batch() call in the same body) MUST NOT appear here -- migrate to
# .peek() instead.
#
# FORMAT (content-keyed, plan-lint-gate-integrity Phase B):
#
#   <path>@<ordinal>:<fingerprint> # <reason> | <the suppressed source line>
#
#   ordinal     which occurrence of that exact line within the file (0-based),
#               so two byte-identical .read() lines stay two distinct sites
#   fingerprint FNV-1a 64 of the whitespace-normalised line, as h<16 hex>
#
# The trailing `| <source line>` is a human-readable echo only; the key is
# everything before the first `#`.
#
# WHY NOT `path:line`: this file used to be line-keyed and 1323 of its 1469
# entries (90%) named a line holding no `.read()` at all. Those suppressions
# were inert while the sites they meant to cover had drifted into
# crates/lint-gate/baseline.json, so an unrelated one-line reflow flipped
# sites between "allowlisted" and "baselined" and manufactured a false
# new-hang-class violation. Content keys survive reflow; line keys do not.
#
# REGENERATE (never hand-edit a fingerprint):
#   cargo test -p poly-lint-gate-rules -- --ignored regenerate_render_time_read_allowlist
#
# The `render_time_read_allowlist` self-check in
# crates/lint-gate-rules/src/forbid_render_time_read.rs fails the gate if any
# entry here stops resolving to a real site, so the inert state cannot recur.
#
# Inline suppression (preferred for one-offs):
#   // poly-lint: allow render-time-read -- <reason>
# See: docs/dev/reactive-state.md section .peek() vs .read()
#      docs/plans/plan-peek-vs-read.md
#      docs/plans/plan-lint-gate-integrity.md Phase B
#
# Migrated HIGH sites (see docs/plans/plan-peek-vs-read.md):
#   - crates/core/src/ui/account/common/thread_view.rs (two .read() keys -> .peek())
#   - crates/core/src/ui/account/common/chat_view.rs use_search_messages_effect (-> .peek())
#   - crates/core/src/ui/account/common/chat_view.rs use_pinned_messages_effect (-> .peek())
crates/core/src/ui/account/common/account_bar.rs@0:h7acc98957bebacfa # MEDIUM — render-time read drives output or child prop, subscription is intent | let nav_snap = nav_state.read().clone();
crates/core/src/ui/account/common/account_bar.rs@0:h8883ac9f2d3eb4e1 # MEDIUM — render-time read drives output or child prop, subscription is intent | let nav = nav_state.read();
crates/core/src/ui/account/common/account_bar.rs@0:hfecaf2434f654056 # MEDIUM — render-time read drives output or child prop, subscription is intent | let cm = client_manager.read();
crates/core/src/ui/account/common/account_switcher.rs@0:he77c4517be5cf56f # MEDIUM — render-time read drives output or child prop, subscription is intent | let voice_conn = voice_state.read().voice_connection.clone();
crates/core/src/ui/account/common/agent_panel.rs@0:h3f14062c1a8a724c # MEDIUM — render-time read drives output or child prop, subscription is intent | if let Some(msg) = copy_status.read().clone() {
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:h169ca99694601c04 # MEDIUM — render-time read drives output or child prop, subscription is intent | let active_account_id = nav.read().active_account_id.cloned();
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:h421e8c37b9680af4 # MEDIUM — render-time read drives output or child prop, subscription is intent | let nav_snap = nav.read();
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:h8ea7a7c2df5c6bf2 # MEDIUM — render-time read drives output or child prop, subscription is intent | let selected_channel = nav.read().selected_channel.clone();
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:h9d2ecc9324082413 # MEDIUM — render-time read drives output or child prop, subscription is intent | .or_else(|| nav_state.read().active_instance_id.cloned())
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:hc04357c766aeb078 # MEDIUM — render-time read drives output or child prop, subscription is intent | let account_id = nav_state.read().active_account_id.cloned()?;
crates/core/src/ui/account/common/channel_list/dm_view.rs@0:hd93af2daee41a462 # MEDIUM — render-time read drives output or child prop, subscription is intent | let previous_channel_id = nav_state.read().selected_channel.cloned();
crates/core/src/ui/account/common/channel_list/dm_view.rs@1:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/dm_view.rs@1:h421e8c37b9680af4 # MEDIUM — render-time read drives output or child prop, subscription is intent | let nav_snap = nav.read();
crates/core/src/ui/account/common/channel_list/dm_view.rs@2:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/dm_view.rs@3:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/dm_view.rs@4:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/dm_view.rs@5:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/items.rs@0:h9478de6d89aea904 # MEDIUM — render-time read drives output or child prop, subscription is intent | let is_collapsed = *collapsed.read();
crates/core/src/ui/account/common/channel_list/mod.rs@0:hbe1eef6329dd6b82 # MEDIUM — render-time read drives output or child prop, subscription is intent | let current_view = *nav.read().view;
crates/core/src/ui/account/common/channel_list/server_view.rs@1:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/channel_list/server_view.rs@2:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/conversation_search_view.rs@0:h0f0c70d40e52415a # MEDIUM — render-time read drives output or child prop, subscription is intent | for group in visible_grps.iter().take(*grp_visible.read()) {
crates/core/src/ui/account/common/conversation_search_view.rs@0:h58661b047c452a74 # MEDIUM — render-time read drives output or child prop, subscription is intent | if enabled_types.read().contains("groups") && !visible_grps.is_empty() {
crates/core/src/ui/account/common/conversation_search_view.rs@0:h6a563e2bf7153b6e # MEDIUM — render-time read drives output or child prop, subscription is intent | if enabled_types.read().contains("dms") && !visible_dms.is_empty() {
crates/core/src/ui/account/common/conversation_search_view.rs@0:h9dbcdf901ef02168 # MEDIUM — render-time read drives output or child prop, subscription is intent | let shown = (*dm_visible.read()).min(dm_total);
crates/core/src/ui/account/common/conversation_search_view.rs@0:hd2be436fdad93fe2 # MEDIUM — render-time read drives output or child prop, subscription is intent | let current = query.read().clone();
crates/core/src/ui/account/common/conversation_search_view.rs@0:hd485eaa59bb5c618 # MEDIUM — render-time read drives output or child prop, subscription is intent | let shown = (*grp_visible.read()).min(grp_total);
crates/core/src/ui/account/common/conversation_search_view.rs@0:hfd3a19ff01c39f8e # MEDIUM — render-time read drives output or child prop, subscription is intent | for dm in visible_dms.iter().take(*dm_visible.read()) {
crates/core/src/ui/account/common/discord_forum_view.rs@0:h0f7bec81f266f656 # MEDIUM — render-time read drives output or child prop, subscription is intent | let cur_sort = *sort.read();
crates/core/src/ui/account/common/discord_forum_view.rs@0:h2bda35491b03b128 # MEDIUM — render-time read drives output or child prop, subscription is intent | let post_body = body.read().clone();
crates/core/src/ui/account/common/discord_forum_view.rs@0:h8bb444b32bfd52af # MEDIUM — render-time read drives output or child prop, subscription is intent | let s = nav.read();
crates/core/src/ui/account/common/discord_forum_view.rs@0:he68de0b58447a3fc # MEDIUM — render-time read drives output or child prop, subscription is intent | let cd = chat_view_state.read();
crates/core/src/ui/account/common/discord_forum_view.rs@1:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/discord_forum_view.rs@2:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/discord_forum_view.rs@3:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/forum_view.rs@0:hf5b3ac967ddc81ad # MEDIUM — render-time read drives output or child prop, subscription is intent | let forum_filter_val = filter_debounced.read().clone();
crates/core/src/ui/account/common/media_picker.rs@0:h2e7b4a5720843f37 # MEDIUM — render-time read drives output or child prop, subscription is intent | let current = *markdown_enabled.read();
crates/core/src/ui/account/common/media_picker.rs@0:h80a9c073087eeb1c # MEDIUM — render-time read drives output or child prop, subscription is intent | let tab = *active_tab.read();
crates/core/src/ui/account/common/media_picker.rs@0:h8ef90e0d4e4c3163 # MEDIUM — render-time read drives output or child prop, subscription is intent | let enabled = *markdown_enabled.read();
crates/core/src/ui/account/common/media_picker.rs@0:hc5b23402289cc35f # MEDIUM — render-time read drives output or child prop, subscription is intent | let sections_ref = sections.read().clone();
crates/core/src/ui/account/common/media_picker.rs@0:hee55463d444fc651 # MEDIUM — render-time read drives output or child prop, subscription is intent | let search = search_text.read().clone();
crates/core/src/ui/account/common/new_conversation_view.rs@0:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/new_conversation_view.rs@0:h1543331d36deff6c # MEDIUM — rsx! string interpolation, subscription is intent | value: "{search_filter.read()}",
crates/core/src/ui/account/common/new_conversation_view.rs@0:h55561bd3904c89c8 # MEDIUM — render-time read drives output or child prop, subscription is intent | let active_backend = nav_state.read().active_backend.cloned();
crates/core/src/ui/account/common/new_conversation_view.rs@0:h7b92015295db0f72 # MEDIUM — render-time read drives output or child prop, subscription is intent | let Some(friend_id) = selected_user_ids.read().first().cloned() else {
crates/core/src/ui/account/common/new_conversation_view.rs@0:h8e431cfbc17b3268 # MEDIUM — render-time read drives output or child prop, subscription is intent | let selected_count = selected_user_ids.read().len();
crates/core/src/ui/account/common/new_conversation_view.rs@0:hd5b120802c478f02 # MEDIUM — render-time read drives output or child prop, subscription is intent | let search_lower = search_filter.read().to_lowercase();
crates/core/src/ui/account/common/new_conversation_view.rs@0:heb1f27f5e6ce4ace # MEDIUM — render-time read drives output or child prop, subscription is intent | let selected = selected_user_ids.read().contains(&friend_id);
crates/core/src/ui/account/common/thread_view.rs@1:h5965be4864cc0d5b # MEDIUM — render-time read drives output or child prop, subscription is intent | let channel = thread_channel.read().as_ref().and_then(std::clone::Clone::clone);
crates/core/src/ui/account/common/user_sidebar.rs@0:h791c8d6adc9c6a5b # MEDIUM — render-time read drives output or child prop, subscription is intent | let sort_order = user_prefs.read().member_list_sort_order;
crates/core/src/ui/account/common/voice_bar.rs@0:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/common/voice_bar.rs@0:h5a1e85f07fa9e298 # MEDIUM — render-time read drives output or child prop, subscription is intent | let noise_cancel = voice_state.read().voice_media_settings.noise_cancel_enabled;
crates/core/src/ui/account/common/voice_bar.rs@0:ha40f16dcb10e0962 # MEDIUM — render-time read drives output or child prop, subscription is intent | let held_count = voice_state.read().held_voice_connections.len();
crates/core/src/ui/account/common/voice_bar.rs@0:he77c4517be5cf56f # MEDIUM — render-time read drives output or child prop, subscription is intent | let voice_conn = voice_state.read().voice_connection.clone();
crates/core/src/ui/account/settings/notifications.rs@0:h06e5199b750a4ba2 # MEDIUM — render-time read drives output or child prop, subscription is intent | notify_streams: *signals.notif_streams.read(),
crates/core/src/ui/account/settings/profile.rs@0:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/settings/profile.rs@0:hae0f4ee4206180ff # MEDIUM — render-time read drives output or child prop, subscription is intent | let as_ = account_sessions.read();
crates/core/src/ui/account/settings/voice_settings.rs@0:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/account/settings/voice_settings.rs@0:h3e1adce5d3e19ca8 # MEDIUM — render-time read drives output or child prop, subscription is intent | SpeakerDevicePicker { devices: spk_devices.read().clone() }
crates/core/src/ui/account/settings/voice_settings.rs@0:h4f337826bf851246 # MEDIUM — render-time read drives output or child prop, subscription is intent | if *test_running.read() { "active" } else { "" },
crates/core/src/ui/account/settings/voice_settings.rs@0:h5a1e85f07fa9e298 # MEDIUM — render-time read drives output or child prop, subscription is intent | let noise_cancel = voice_state.read().voice_media_settings.noise_cancel_enabled;
crates/core/src/ui/account/settings/voice_settings.rs@0:hbceb1f80c3070864 # MEDIUM — rsx! attr binding, subscription is intent | disabled: *test_running.read(),
crates/core/src/ui/account/settings/voice_settings.rs@0:he38ae0d365124bb3 # MEDIUM — render-time read drives output or child prop, subscription is intent | MicDevicePicker { devices: mic_devices.read().clone() }
crates/core/src/ui/account/settings/voice_settings.rs@0:heb583b75c449f68c # MEDIUM — render-time read drives output or child prop, subscription is intent | if *test_running.read() {
crates/core/src/ui/account/settings/voice_settings.rs@1:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/agent/chat_style_editor.rs@0:h76ae24e9b1bac828 # MEDIUM — render-time read drives output or child prop, subscription is intent | let n_v = extra_notes.read().clone();
crates/core/src/ui/agent/chat_style_editor.rs@0:hfc15e03b381b5669 # MEDIUM — render-time read drives output or child prop, subscription is intent | let s_v = signature.read().clone();
crates/core/src/ui/client_ui/sidebar/communities.rs@0:h1f32f9adaae1ba95 # MEDIUM — render-time read drives output or child prop, subscription is intent | let account_id = nav.read().active_account_id.cloned();
crates/core/src/ui/client_ui/sidebar/feed.rs@0:h1f32f9adaae1ba95 # MEDIUM — render-time read drives output or child prop, subscription is intent | let account_id = nav.read().active_account_id.cloned();
crates/core/src/ui/client_ui/sidebar/feed.rs@0:he0b0c4e1a3faf74d # MEDIUM — render-time read drives output or child prop, subscription is intent | let current = active_feed.read().clone();
crates/core/src/ui/client_ui/sidebar/repo_tree.rs@0:h1f32f9adaae1ba95 # MEDIUM — render-time read drives output or child prop, subscription is intent | let account_id = nav.read().active_account_id.cloned();
crates/core/src/ui/client_ui/toast.rs@0:hfe37e8408dd03644 # MEDIUM — render-time read drives output or child prop, subscription is intent | let items = queue.read().clone();
crates/core/src/ui/client_ui/view/card_body.rs@0:hd6ba58ae341fd445 # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | let s = nav_sig.read();
crates/core/src/ui/client_ui/view/tree_body.rs@0:h52ac85f19d89d305 # MEDIUM — render-time read drives output or child prop, subscription is intent | let sort_id = selected_sort.read().clone();
crates/core/src/ui/client_ui/view/tree_body.rs@0:h9a6468225879707d # MEDIUM — render-time read drives output or child prop, subscription is intent | let filter_id = selected_filter.read().clone();
crates/core/src/ui/client_ui/view/tree_body.rs@0:hb22b8383c7e5627b # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | let tab_id = selected_tab.read().clone();
crates/core/src/ui/code_explorer.rs@0:h10ee27ccd136b4fd # MEDIUM — render-time read drives output or child prop, subscription is intent | let path = current_path.read().clone();
crates/core/src/ui/create_channel.rs@0:h97c6c4ec0f30c33e # MEDIUM — render-time read drives output or child prop, subscription is intent | let name = channel_name.read().trim().to_string();
crates/core/src/ui/create_server.rs@0:h9f52de29fc77d306 # MEDIUM — rsx! attr binding, subscription is intent | if !error_msg.read().is_empty() {
crates/core/src/ui/demo.rs@0:h388686080356de8b # MEDIUM — render-time read drives output or child prop, subscription is intent | let demo_ids = client_manager.read().demo_account_ids();
crates/core/src/ui/demo.rs@0:h3b523abe47f86f03 # MEDIUM — render-time read drives output or child prop, subscription is intent | if client_manager.read().demo_active {
crates/core/src/ui/demo.rs@0:ha1224eefc6c0fc1b # MEDIUM — render-time read drives output or child prop, subscription is intent | let is_active = client_manager.read().demo_active;
crates/core/src/ui/dialogs/edit_channel.rs@0:h085ef0947592a190 # MEDIUM — render-time read drives output or child prop, subscription is intent | if *submitting.read() { return; }
crates/core/src/ui/dialogs/edit_channel.rs@0:h2e42a37848b6e21e # MEDIUM — render-time read drives output or child prop, subscription is intent | let topic_str = topic.read().trim().to_string();
crates/core/src/ui/dialogs/edit_channel.rs@0:h52d50a75eed6b480 # MEDIUM — rsx! attr binding, subscription is intent | disabled: *submitting.read(),
crates/core/src/ui/dialogs/edit_channel.rs@0:h5b2a8c7182258b5e # MEDIUM — render-time read drives output or child prop, subscription is intent | let name_str = name.read().trim().to_string();
crates/core/src/ui/dialogs/edit_channel.rs@0:h5bf60b3696810b89 # MEDIUM — render-time read drives output or child prop, subscription is intent | if *success.read() {
crates/core/src/ui/dialogs/edit_channel.rs@0:h9f52de29fc77d306 # MEDIUM — render-time read drives output or child prop, subscription is intent | if !error_msg.read().is_empty() {
crates/core/src/ui/dialogs/edit_channel.rs@0:ha23cadf2720115b2 # MEDIUM — render-time read drives output or child prop, subscription is intent | slowmode.read().trim().parse::<u32>().ok()
crates/core/src/ui/dialogs/edit_channel.rs@0:ha72849196fe6d7a8 # MEDIUM — render-time read drives output or child prop, subscription is intent | if !*success.read() {
crates/core/src/ui/electron_titlebar.rs@0:h7acc98957bebacfa # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | let nav_snap = nav_state.read().clone();
crates/core/src/ui/electron_titlebar.rs@0:hd50876690ac7ac64 # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | if !*is_electron.read() {
crates/core/src/ui/electron_titlebar.rs@0:hdd5c2b1884aca103 # MEDIUM — render-time read drives output or child prop, subscription is intent | let as_snap = account_sessions.read().clone();
crates/core/src/ui/main_layout.rs@0:h11c293c42d72cbb4 # MEDIUM — render-time read drives output or child prop, subscription is intent | let dm_routes_snapshot = nav.read().account_last_dm_routes.clone();
crates/core/src/ui/main_layout.rs@0:h12399a7583fe34a0 # MEDIUM — render-time read drives output or child prop, subscription is intent | let has_stack = !ui_overlays.read().context_menu_stack.is_empty();
crates/core/src/ui/main_layout.rs@0:h380e383e16933c6a # MEDIUM — render-time read drives output or child prop, subscription is intent | let routes_snapshot = nav.read().account_last_routes.clone();
crates/core/src/ui/main_layout.rs@0:h80d641a7bf778b07 # MEDIUM — render-time read drives output or child prop, subscription is intent | if route_targets_unknown_account(&r, &client_manager.read()) {
crates/core/src/ui/search.rs@0:h6a563e2bf7153b6e # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | if enabled_types.read().contains("dms") && !visible_dms.is_empty() {
crates/core/src/ui/search.rs@0:h7029214c71959958 # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | client_manager.read().active_account_ids().into_iter().collect()
crates/core/src/ui/search.rs@0:hc1f5e9d8b0d1404d # MEDIUM — render-time read drives output or child prop, subscription is intent | if enabled_types.read().contains("servers") {
crates/core/src/ui/search.rs@0:hcc307175c0aedc7d # MEDIUM — render-time read drives output or child prop, subscription is intent | let checked = enabled_types.read().contains(*type_key);
crates/core/src/ui/search.rs@0:hfecaf2434f654056 # MEDIUM — render-time read drives output or child prop, subscription is intent | let cm = client_manager.read();
crates/core/src/ui/settings/accounts.rs@0:hd64b1ce3ca054504 # MEDIUM — render-time read drives output or child prop, subscription is intent | let account_ids = client_manager.read().active_account_ids();
crates/core/src/ui/settings/backup.rs@0:h69191fea1cb0b85f # MEDIUM — render-time read drives output or child prop, subscription is intent | let pass = wizard_pass.read().clone();
crates/core/src/ui/settings/backup.rs@0:h8b257e086b558250 # MEDIUM — render-time read drives output or child prop, subscription is intent | let pass_req = *wizard_pass_required.read();
crates/core/src/ui/settings/backup.rs@0:h95d3e20318d67e76 # MEDIUM — render-time read drives output or child prop, subscription is intent | let name = wizard_name.read().trim().to_string();
crates/core/src/ui/settings/diagnostics.rs@0:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/settings/diagnostics.rs@0:h99c51a6b04bc0bdc # MEDIUM — render-time read drives output or child prop, subscription is intent | let demo_active = client_manager.read().demo_active;
crates/core/src/ui/settings/diagnostics.rs@1:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/settings/diagnostics.rs@2:h141fc1b57b2e3296 # MEDIUM — render-time read drives output or child prop, subscription is intent | .read()
crates/core/src/ui/settings/identity.rs@0:h9f02d1ad5fe113a2 # MEDIUM — render-time read drives output or child prop, subscription is intent | account_id: modal_account_id.read().clone(),
crates/core/src/ui/settings/language.rs@0:hf475d0a2cf306291 # MEDIUM — render-time read drives output or child prop, subscription is intent | let current_locale = locale_sig.read().clone();
crates/core/src/ui/settings/media.rs@0:h032f2a431cfe74d2 # MEDIUM — render-time read drives output or child prop, subscription is intent | let current = media.read().clone();
crates/core/src/ui/settings/media.rs@0:h3bdc62dd37dda6b8 # MEDIUM — render-time read drives output or child prop, subscription is intent | let mut next = media.read().clone();
crates/core/src/ui/settings/media.rs@0:h5fcbf3911d263eac # MEDIUM — render-time read drives output or child prop, subscription is intent | let is_active = *active_tab.read() == kind;
crates/core/src/ui/settings/media.rs@0:h80a9c073087eeb1c # MEDIUM — render-time read drives output or child prop, subscription is intent | let tab = *active_tab.read();
crates/core/src/ui/settings/media.rs@0:hc68521271de88047 # MEDIUM — render-time read drives output or child prop, subscription is intent | let provider = media.read().active_gif_provider;
crates/core/src/ui/settings/media.rs@0:hcb4781e57b97b96b # MEDIUM — render-time read drives output or child prop, subscription is intent | if *loaded.read() {
crates/core/src/ui/settings/plugins.rs@0:h6a62833e1478e656 # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | p { class: "plugin-add-error", "{error.read()}" }
crates/core/src/ui/settings/theme.rs@0:h11e3e63fb5b24ab5 # MEDIUM — render-time read drives output or child prop, subscription is intent | let exported = crate::theme::export_theme(&theme_config.read());
crates/core/src/ui/settings/theme.rs@0:h11eb8919f955cf40 # MEDIUM — render-time read drives output or child prop, subscription is intent | value: local_css.read().clone(),
crates/core/src/ui/settings/theme.rs@0:h60b7f989400aa28f # MEDIUM — render-time read drives output or child prop, subscription is intent | let css = local_css.read().clone();
crates/core/src/ui/settings/theme.rs@0:h7279b5c87cf49cfb # MEDIUM — render-time read drives output or child prop, subscription is intent | let config = theme_config.read().clone();
crates/core/src/ui/settings/theme.rs@0:h8de3cd2bf4ed344e # MEDIUM — render-time read drives output or child prop, subscription is intent | let current = theme_config.read().preset.canonical();
crates/core/src/ui/settings/theme.rs@0:h92f23abee1ef2f3f # MEDIUM — render-time read drives output or child prop, subscription is intent | let current = theme_config.read().color_mode;
crates/core/src/ui/settings/theme.rs@0:h99e8b3152dccf369 # MEDIUM — render-time read drives output or child prop, subscription is intent | let mut cfg = theme_config.read().clone();
crates/core/src/ui/settings/theme.rs@0:ha960e77e2c3c7536 # MEDIUM — render-time read drives output or child prop, subscription is intent | let _locale = crate::i18n::use_locale().read().clone();
crates/core/src/ui/settings/theme.rs@1:h60b7f989400aa28f # MEDIUM — render-time read drives output or child prop, subscription is intent | let css = local_css.read().clone();
crates/core/src/ui/settings/theme.rs@1:h7279b5c87cf49cfb # MEDIUM — render-time read drives output or child prop, subscription is intent | let config = theme_config.read().clone();
crates/core/src/ui/settings/theme.rs@1:h99e8b3152dccf369 # MEDIUM — render-time read drives output or child prop, subscription is intent | let mut cfg = theme_config.read().clone();
crates/core/src/ui/settings/theme.rs@1:ha960e77e2c3c7536 # MEDIUM — render-time read drives output or child prop, subscription is intent | let _locale = crate::i18n::use_locale().read().clone();
crates/core/src/ui/settings/theme.rs@2:h99e8b3152dccf369 # MEDIUM — render-time read drives output or child prop, subscription is intent | let mut cfg = theme_config.read().clone();
crates/core/src/ui/settings/theme.rs@2:ha960e77e2c3c7536 # MEDIUM — render-time read drives output or child prop, subscription is intent | let _locale = crate::i18n::use_locale().read().clone();
crates/core/src/ui/settings/theme.rs@3:ha960e77e2c3c7536 # MEDIUM — render-time read drives output or child prop, subscription is intent | let _locale = crate::i18n::use_locale().read().clone();
crates/core/src/ui/settings/translation.rs@0:hbcfd411ffbb75cec # MEDIUM — render-time read drives output or child prop, subscription is intent | let status = browser_status.read().clone();
crates/core/src/ui/settings/voice_video.rs@0:h244eadbbecd7dc75 # MEDIUM — render-time read drives output or child prop, subscription is intent | echo_cancellation: *echo_cancel.read(),
crates/core/src/ui/settings/voice_video.rs@0:h647f6d97074de8c4 # MEDIUM — render-time read drives output or child prop, subscription is intent | noise_suppression: noise_suppress.read().clone(),
crates/core/src/ui/settings/voice_video.rs@0:h84c6074f019740a5 # MEDIUM — render-time read drives output or child prop, subscription is intent | input_mode: vad_mode.read().clone(),
crates/core/src/ui/settings/voice_video.rs@0:ha97b1971d35510a5 # MEDIUM — render-time read drives output or child prop, subscription is intent | output_volume: *output_vol.read(),
crates/core/src/ui/signup.rs@0:hb88d15afd2d2ef83 # MEDIUM — render-time read; subscription or async-body, not a hang-class-7 trigger | let manager = client_manager.read();
