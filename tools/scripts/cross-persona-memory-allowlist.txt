# cross-persona-memory-allowlist.txt — Phase Q.1 of plan-persona-quality-gates.md.
#
# File-level fallback allowlist for `forbid-cross-persona-memory.sh`.
# Use this when a cross-persona-scoped query is intentional and safe —
# e.g. administrative housekeeping or primary-key targeted deletes whose
# callers already scoped the IDs to the correct persona.
#
# Formats (first match wins):
#   path                      whole-file allow (use sparingly)
#   path:N                    pin a single line (line-drift risk)
#   path:start-end            range allow (preferred)
#
# Every entry MUST carry a trailing `# rationale` or a preceding
# `#`-comment block explaining WHY the cross-persona access is safe.
# Adding an entry without rationale will be rejected in code review.
#
# The preferred mechanism is an inline comment on the offending line:
#   // poly-lint: allow cross-persona-memory — <reason>

# `prune_persona_audit_before` is now allow-listed via inline comment
# on the SQL string itself — more line-drift-resistant than a pinned
# number. See `mcp/chat-mcp/src/memory.rs` near the
# `prune_persona_audit_before` fn for the rationale.
